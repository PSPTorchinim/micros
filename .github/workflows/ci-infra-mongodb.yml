name: CI - Infra MongoDB

# Triggers on changes to the MongoDB Dockerfile,
# linting it to catch issues before they reach the CD pipeline.

on:
  push:
    branches:
      - develop
    paths:
      - 'Docker/infra/mongodb.Dockerfile'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint MongoDB Dockerfile
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v5

      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Docker/infra/mongodb.Dockerfile
