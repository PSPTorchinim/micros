name: PR Validation

# This workflow validates pull requests before merging:
# 1. Builds all .NET microservices to check for compilation errors
# 2. Builds React frontends and runs tests (unit + E2E)
# 3. Lints all Dockerfiles for best practices
# 4. Validates Docker Compose configurations
# 5. Lints GitHub Actions workflow files
# All checks must pass before PR can be merged

on:
  pull_request:

jobs:
  determine_environment:
    runs-on: ubuntu-latest
    name: Determine Environment
    outputs:
      current_env: ${{ steps.set_env.outputs.current_env }}
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v5
      - name: Set Environment
        id: set_env
        uses: ./.github/templates/determine-environment

  determine_docker_tag:
    name: Determine Docker Tag
    runs-on: ubuntu-latest
    outputs:
      docker_tag: ${{ steps.tag_set.outputs.docker_tag }}
      latest_tag: ${{ steps.tag_set.outputs.latest_tag }}
      all_tags: ${{ steps.tag_set.outputs.all_tags }}
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v5
      - name: Determine Docker tag based on branch
        id: tag_set
        uses: ./.github/templates/determine-docker-tag

  validate_dotnet:
    name: Build - DotNet
    runs-on: ubuntu-latest
    needs: determine_environment
    environment: ${{ needs.determine_environment.outputs.current_env }}
    timeout-minutes: 15
    strategy:
      matrix:
        service:
          [
            "CompanyAPI",
            "DJHostGateway",
            "DocumentsAPI",
            "EquipmentAPI",
            "IdentityAPI",
            "MailingAPI",
            "MusicAPI",
            "PartyAPI",
          ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Build .NET Service
        uses: ./.github/templates/build-dotnet-service
        with:
          service: ${{ matrix.service }}

  validate_react:
    name: Build - React
    needs: determine_environment
    environment: ${{ needs.determine_environment.outputs.current_env }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        service: ["dj-panel"]
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Build React Service
        uses: ./.github/templates/build-react-service
        with:
          service: ${{ matrix.service }}

  validate_dockerfiles:
    name: Validate Dockerfiles
    needs: determine_environment
    runs-on: ubuntu-latest
    environment: ${{ needs.determine_environment.outputs.current_env }}
    timeout-minutes: 10
    env:
      DATABASE_USER_MONGODB: ${{ vars.DATABASE_USER_MONGODB }}
      DATABASE_PASSWORD_MONGODB: ${{ secrets.DATABASE_PASSWORD_MONGODB }}
      CMS_DATABASE_NAME: ${{ vars.CMS_DATABASE_NAME }}
      CMS_DATABASE_USERNAME: ${{ vars.CMS_DATABASE_USERNAME }}
      CMS_DATABASE_PASSWORD: ${{ secrets.CMS_DATABASE_PASSWORD }}
      CMS_DATABASE_CLIENT: ${{ vars.CMS_DATABASE_CLIENT }}
      CMS_NODE_ENV: ${{ vars.CMS_NODE_ENV }}
      CMS_DATABASE_HOST: ${{ vars.CMS_DATABASE_HOST }}
      CMS_DATABASE_PORT: ${{ vars.CMS_DATABASE_PORT }}
      CMS_JWT_SECRET: ${{ secrets.CMS_JWT_SECRET }}
      CMS_ADMIN_JWT_SECRET: ${{ secrets.CMS_ADMIN_JWT_SECRET }}
      CMS_APP_KEYS: ${{ secrets.CMS_APP_KEYS }}
      REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
      RABBITMQ_USER: ${{ vars.RABBITMQ_USER }}
      RABBITMQ_PASSWORD: ${{ secrets.RABBITMQ_PASSWORD }}
      DATABASE_PASSWORD_SQLSERVER: ${{ secrets.DATABASE_PASSWORD_SQLSERVER }}
      CMS_API_TOKEN_SALT: ${{ secrets.CMS_API_TOKEN_SALT }}
      GRAFANA_ADMIN_USER: ${{ vars.GRAFANA_ADMIN_USER }}
      GRAFANA_ADMIN_PASSWORD: ${{ secrets.GRAFANA_ADMIN_PASSWORD }}
      GRAFANA_HOST: ${{ vars.GRAFANA_HOST }}
      GRAFANA_PORT: ${{ vars.GRAFANA_PORT }}
      GRAFANA_PROTOCOL: ${{ vars.GRAFANA_PROTOCOL }}
    strategy:
      matrix:
        file:
          [
            "services/dotnet",
            "frontends/react",
            "infra/mongodb",
            "infra/postgres",
            "infra/redis",
            "infra/rabbitmq",
            "infra/sqlserver",
            "infra/strapi",
            "infra/storybook",
            "infra/grafana",
            "infra/promtail",
            "infra/loki",
          ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Hadolint Dockerfiles
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Docker/${{ matrix.file }}.Dockerfile

      - name: Build and Push Docker Image
        uses: ./.github/templates/docker-build-push
        with:
          service-name: ${{ matrix.image.name }}
          service-type: infra
          docker-tag: ${{ needs.determine_docker_tag.outputs.docker_tag }}
          latest-tag: ${{ needs.determine_docker_tag.outputs.latest_tag }}
          dockerfile: ${{ matrix.image.dockerfile }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository-owner: ${{ github.repository_owner }}
          # Pass ALL possible args; Docker will ignore ones not declared via ARG
          build-args: |
            DATABASE_USER_MONGODB=${{ env.DATABASE_USER_MONGODB }}
            DATABASE_PASSWORD_MONGODB=${{ env.DATABASE_PASSWORD_MONGODB }}
            DATABASE_NAME_POSTGRES=${{ env.CMS_DATABASE_NAME }}
            DATABASE_USERNAME_POSTGRES=${{ env.CMS_DATABASE_USERNAME }}
            DATABASE_PASSWORD_POSTGRES=${{ env.CMS_DATABASE_PASSWORD }}
            CMS_DATABASE_CLIENT=${{ env.CMS_DATABASE_CLIENT }}
            CMS_NODE_ENV=${{ env.CMS_NODE_ENV }}
            CMS_DATABASE_HOST=${{ env.CMS_DATABASE_HOST }}
            CMS_DATABASE_PORT=${{ env.CMS_DATABASE_PORT }}
            CMS_JWT_SECRET=${{ env.CMS_JWT_SECRET }}
            CMS_ADMIN_JWT_SECRET=${{ env.CMS_ADMIN_JWT_SECRET }}
            CMS_APP_KEYS=${{ env.CMS_APP_KEYS }}
            CMS_API_TOKEN_SALT=${{ secrets.CMS_API_TOKEN_SALT }}
            REDIS_PASSWORD=${{ env.REDIS_PASSWORD }}
            RABBITMQ_USER=${{ env.RABBITMQ_USER }}
            RABBITMQ_PASSWORD=${{ env.RABBITMQ_PASSWORD }}
            DATABASE_PASSWORD_SQLSERVER=${{ env.DATABASE_PASSWORD_SQLSERVER }}
            GRAFANA_ADMIN_USER=${{ env.GRAFANA_ADMIN_USER }}
            GRAFANA_ADMIN_PASSWORD=${{ env.GRAFANA_ADMIN_PASSWORD }}
            PROJECT_NAME=ix-dj-panel-${{ needs.determine_environment.outputs.env_slug }}
            REPLICA_INDEX=1

  validate_composers:
    name: Validate Docker Compose files
    needs: determine_environment
    environment: ${{ needs.determine_environment.outputs.current_env }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Validate Docker Compose YAML
        run: |
          docker compose -f Docker/dj-panel-composer.yml config

  validate_github_workflows:
    name: Validate GitHub Actions workflows
    runs-on: ubuntu-latest
    needs: determine_environment
    environment: ${{ needs.determine_environment.outputs.current_env }}
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Lint workflows with actionlint
        uses: raven-actions/actionlint@v1

  report_status:
    name: PR Validation
    runs-on: ubuntu-latest
    needs:
      - validate_dotnet
      - validate_react
      - validate_dockerfiles
      - validate_composers
      - validate_github_workflows
    if: always()
    steps:
      - name: Report overall status
        run: |
          echo "=========================================="
          echo "PR Validation Status Report"
          echo "=========================================="
          echo "DotNet Build:        ${{ needs.validate_dotnet.result }}"
          echo "React Build:         ${{ needs.validate_react.result }}"
          echo "Dockerfile Lint:     ${{ needs.validate_dockerfiles.result }}"
          echo "Compose Validation:  ${{ needs.validate_composers.result }}"
          echo "Workflow Lint:       ${{ needs.validate_github_workflows.result }}"
          echo "=========================================="

          if [ "${{ needs.validate_dotnet.result }}" = "success" ] && \
             [ "${{ needs.validate_react.result }}" = "success" ] && \
             [ "${{ needs.validate_dockerfiles.result }}" = "success" ] && \
             [ "${{ needs.validate_composers.result }}" = "success" ] && \
             [ "${{ needs.validate_github_workflows.result }}" = "success" ]; \
          then
            echo "✅ All validation jobs succeeded."
            exit 0
          else
            echo "❌ One or more validation jobs failed."
            exit 1
          fi
