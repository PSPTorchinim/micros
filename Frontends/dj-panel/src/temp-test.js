// Test file with intentional formatting issues
const badlyFormatted = 'test';
const anotherVar = 'no spaces';

function testFunction() {
  return badlyFormatted + anotherVar;
}

export default testFunction;
