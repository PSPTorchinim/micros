{{- range .Values.postgres }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .name }}-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ $.Values.storage.postgres | default "5Gi" }}
---
{{- end }}
