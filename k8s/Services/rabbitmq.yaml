apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
spec:
  type: NodePort
  ports:
    - port: 5672
      targetPort: 5672
      nodePort: 40004
    - port: 15672
      targetPort: 15672
      nodePort: 40014
  selector:
    app: rabbitmq
