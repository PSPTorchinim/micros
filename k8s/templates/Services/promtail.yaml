{{- range .Values.promtail.services }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
spec:
  type: ClusterIP
  ports:
    - port: 9080
      targetPort: 9080
  selector:
    app: {{ .name }}
{{- end }}