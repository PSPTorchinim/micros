{{- range .Values.strapi.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
spec:
  type: {{- if .nodePort }} NodePort {{- else }} ClusterIP {{- end }}
  ports:
    - port: 1337
      targetPort: 1337
      {{- if .nodePort }}
      nodePort: {{ .nodePort }}
      {{- end }}
  selector:
    app: {{ .name }}
{{- end }}
